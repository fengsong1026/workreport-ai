---
alwaysApply: true
scene: git_message
---

CRITICAL: All commit messages MUST be written in English ONLY. Never use Chinese or any other language in commit messages. This is a strict requirement that overrides any conversational language preference.
